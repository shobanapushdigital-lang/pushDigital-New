import React from "react";
import Projects from "../Componts/Projects";

export const metadata = {
  title: "Projects | Push Digital",
  description:
    "Push Digital's projects and case studies.",
};

function ProjectsPage() {
  return <Projects />;
}

export default ProjectsPage;